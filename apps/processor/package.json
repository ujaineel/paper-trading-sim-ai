{
  "name": "processor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.js --external:aws-sdk/*",
    "build:zip": "rm -rf dist && tsc && mkdir -p ../../terraform/dist && cp package.json dist/ && cd dist && pnpm install --prod && zip -r ../../../terraform/dist/processor.zip ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.29.3",
  "dependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/sentiment": "^5.0.4",
    "sentiment": "^5.0.2"
  },
  "devDependencies": {
    "esbuild": "0.27.3"
  }
}
